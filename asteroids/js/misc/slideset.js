
var rslidebar = createSlider(0, 360);
var gslidebar=createSlider(0, windowWidth);
var bslidebar=createSlider(0, 255);
var aslidebar=createSlider(0, ast);


//slidderRate = createSlider(0, 3, 1, 0.01);
var slidderVolume0 = createSlider(0, 2, 4, 6);