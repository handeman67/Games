// Physics build up.Phsudo code 

// userInput;
// position;

// velocity;
// accelleration;
// gravity;
collisionDetect: {
    collisionType: {
        bounce: bounceDetect = () => {
            console.log('bounceDetect');
        };
        normal: normalDetect = () => {
            console.log('normalDetect');
        }
        collisionTransfer: {
            let object = 1;
            let friction = 0.02
            object - object * friction;
        };


    };
}